<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToolTrack.Pages.VerInventarioPage"
             Title="Inventario">

    <ScrollView>
        <VerticalStackLayout Padding="20" x:Name="InventarioLayout" Spacing="10">
        </VerticalStackLayout>
    </ScrollView>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Volver" Clicked="OnVolverClicked"/>
    </ContentPage.ToolbarItems>
</ContentPage>
